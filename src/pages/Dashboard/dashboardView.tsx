import { Grid } from "@mantine/core";
import { useForm } from "@mantine/form";

export default function DashboardView() {
  return (
    <Grid>
      <Grid.Col span={1}>
        <div>Dashboard</div>
      </Grid.Col>
    </Grid>
  );
}
